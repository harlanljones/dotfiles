# HJ-738 Evidence — Review harness-config-opencode-skills: impeccable

Date: 2026-09-09. Scope: HJ-738, the single frozen full-tree variant
`library/harness-config-opencode-skills/impeccable/82d09480e480`
(148 baseline files, tree
`82d09480e480d48c0f66f218081b507a20e5143998d407ff1ddd71c94185a661`).
Worker authored and then re-ran the scoped checks below. This is review
readiness, not human approval or deployment. No Linear mutation; the parent
dispatcher owns tracker state. No candidate workflow was run against live
services; no paid model or image calls were made.

## Checkpoint Coverage

- Prior worker (transcript: `/tmp/opencode/frontier-logs/HJ-738-export.md`):
  issue/rules intake, Linear body + 4 comments + relations, inventory
  confirmation (148 files = SKILL.md + 39 references + 108 scripts,
  3,255,305 bytes), full 39-file reference pass, and scripts 0–35 of the
  108-script effect checkpoint.
- This worker: re-read the current Linear issue (In Progress, no open
  prerequisite, blocks HJ-853 only), README/RUBRIC/VERIFICATION, completed the
  remaining 72-script effect checkpoint (read/write/delete/subprocess/network/
  env/server/browser/auth flags for all 108 scripts), and directly read
  SKILL.md, command-metadata.json, generate-image.mjs, hook/context/live
  scripts and heads, the small references, and provider/dependency signals.
- No pass was redone; no baseline file was skipped as "unchanged".

## Executed Checks

From the worktree root with `PYTHONDONTWRITEBYTECODE=1`:

- `python3 docs/skill-review/review.py check --source
  harness-config-opencode-skills --skill impeccable`: 1 selected variant, 1
  contract-valid, 0 invalid, 0 missing files, 0 layout errors,
  `allSelectedContractsValid: true`. This verifies declared coverage and
  preservation, not semantic reading by itself.
- `python3 docs/skill-review/test_review.py`: 34 fixture tests pass.
- `node --check` on the three edited `.mjs` files
  (`scripts/generate-image.mjs`, `scripts/live-server.mjs`,
  `scripts/hook-lib.mjs`): exit 0, Node v26.8.1. Syntax only; candidate logic
  was not executed.
- `generate-image.mjs` fake-mode smoke (`IMPECCABLE_IMAGE_GEN_FAKE=1`) in
  `/tmp/opencode/hj738`: valid 64x64 PNG (magic bytes, `SYNTHETIC` marker),
  `$0.00`, no key, no network; rerun byte-identical. This exercises the
  script's built-in offline stand-in, not the paid OpenAI path.

## Static Walkthroughs (not executed tests)

No-arg routing returns a context-aware menu without auto-running; an explicit
or clearly implied command loads its reference; comp-led builds block on code
until a recorded approval exists; `config_missing`/`config_invalid` routes to
`live-setup.md`; hook triage resolves to fix, narrowest evidenced ignore, or
one question; the finish reviewer returns exactly one of four disposition
words; the documenter never canonizes craft-floor bans into `DESIGN.md`.

## Source-Grounded Limits

- Provenance: local installed tree
  `/home/harlan/.config/opencode/skills/impeccable`, upstream unknown,
  version 4.1.1 from frontmatter. Source-mirror originals hash-match on this
  machine (spot-checked SKILL.md, live.mjs, generate-image.mjs, live.md,
  command-metadata.json); fresh-clone portability remains unresolved.
- License: `SKILL.md:7` declares Apache 2.0; no license file exists in the
  tree (inventory: "no license file found"); permission unknown
  (`Q-impeccable-license`).
- Dependencies: Node.js runtime; zero npm dependencies (no package.json);
  optional `OPENAI_API_KEY` spend (`Q-impeccable-cost`); optional
  `@babel/parser` with graceful fallback; `impeccable.style/api` roll catalog
  with 4s shared deadline and degraded seed; external `npx impeccable` CLI.
- Live loopback server never launched; hook never installed; five questions
  (`Q-impeccable-cost`, `Q-impeccable-live`, `Q-impeccable-hook`,
  `Q-impeccable-license`, `Q-impeccable-pack`) and core/pack membership remain
  explicit human decisions.
