# HJ-741 Evidence — Review harness-pi-agent-skills: impeccable

Date: 2026-09-09. Scope: the single frozen full-tree variant
`library/harness-pi-agent-skills/impeccable/fa8118279b98`
(`impeccable:fa8118279b98…15ed4`, 148 baseline files). Continued from the
prior worker transcript (`/tmp/opencode/frontier-logs/HJ-741-export.md`:
full-tree read passes done, validation starting); read passes were completed
here, not redone from zero. No Linear mutation (parent dispatcher owns
tracker state). No install, apply, commit, push, PR creation, or paid model
calls. No candidate workflow run against live services.

## Pass structure (checkpointed full-tree work)

1. Enumeration: 148 files / 20 directories via `find … -type f | sort`
   (`/tmp/opencode/hj741-files.txt`); no symlinks, no empty files.
   Counts: 40 `.md`, 102 `.mjs`, 5 `.js`, 1 `.json`.
2. Baseline: `review.py status --source harness-pi-agent-skills --skill
   impeccable` → unreviewed, no missing files; SKILL.md sha256 matches the
   frozen inventory (`a0e577e0…ca5c`); all 148 live-mirror hashes under
   `/home/harlan/.pi/agent/skills/impeccable` re-verified matching before
   any edit.
3. Reference passes: full reads of SKILL.md, hooks.md, doctor.md,
   craft-floor.md, operate.md, new-work.md, live.md (323 lines), live-setup.md,
   routing.md, ios.md, init.md, degraded/×4 (headers + full short files);
   section-header extraction + targeted reads for adapt/adapt.native/android/
   animate/audit/audit.native/bolder/clarify (prior pass) and
   colorize/critique/delight/distill/document/extract/harden/layout/onboard/
   optimize/overdrive/polish/quieter/shape/typeset/visualize (this pass).
4. Script passes: full reads of generate-image.mjs, provider.mjs,
   command-metadata.json, hook.mjs head, live-copy-edit-agent.mjs §175–210,
   concept-seed.mjs head + tail; import/env/network/fs-write sweeps over all
   scripts; license-header spot checks.

## Key source-grounded findings

- Pi build: `scripts/lib/provider.mjs` pins `IMPECCABLE_PROVIDER_ID = "pi"`,
  `/impeccable` prefix; `allowed-tools` allows `npx impeccable *` and
  `node .pi/skills/impeccable/scripts/*`. On pi no design hook runs
  (new-work §7: hookless web build runs `detect.mjs` once instead).
- License gap: frontmatter claims `license: Apache 2.0`, but no LICENSE
  file exists in-tree; inventory licenseStatus unknown
  → Q-impeccable-license (SKILL.md).
- Live authority: helper binds localhost (default port 8400), injects page
  script, offers dev-only CSP patch with a verbatim consent prompt, writes
  user source on accept with carbonize cleanup gate
  → Q-impeccable-live-authority (reference/live.md).
- Hooks: per-project detector hook; Cursor `preToolUse` can block writes;
  narrowest-ignore triage ladder → Q-impeccable-hooks (reference/hooks.md).
- Image cost: `generate-image.mjs` uses the user's OPENAI_API_KEY with
  gpt-image-2 (~$0.05–0.25/image), states cost before first call,
  harness-native wins when present → Q-impeccable-image-cost.
- Telemetry: `concept-seed.mjs` GETs `https://impeccable.style/api/roll`
  plus one choice ping per resolved round; DO_NOT_TRACK /
  IMPECCABLE_NO_TELEMETRY opt out; offline degraded path exists.
  `context.mjs` update check honors IMPECCABLE_NO_UPDATE_CHECK + config
  `updateCheck` → Q-impeccable-telemetry.
- Deps: runtime imports are node builtins only; `svelte`/`react` imports are
  generated template strings for user projects; `@babel/parser` is an
  optional require with `syntax_parser_unavailable` warning fallback.
- Absent from candidate (recorded, not waived): upstream version/changelog,
  `npx impeccable` CLI, `tests/` fixtures named by live.md
  (`tests/live-e2e/agent.mjs`, `tests/framework-fixtures/*`).

## Executed checks (exact commands, worktree root unless noted)

From `docs/skill-review` unless noted; all with `PYTHONDONTWRITEBYTECODE=1`:

- `python3 docs/skill-review/review.py check --source harness-pi-agent-skills --skill impeccable` → exit 0, `allSelectedContractsValid: true`, changedPaths exactly the 7 review-touched paths (5 question-ID files + REVIEW.md + review.json), no errors.
- `IMPECCABLE_IMAGE_GEN_FAKE=1 node <candidate>/scripts/generate-image.mjs --prompt "fake fixture hero" --out /tmp/opencode/hj741-fixture/fake.png` → exit 0, `IMAGE: … (1536x1024, fake synthetic comp, $0.00, no API call)`; `file` confirms `PNG image data, 1536 x 1024, 8-bit/color RGB`. Offline, no key, no network. Fixture output left in /tmp, not the repo.
- `node --check` over all 102 `.mjs` + non-bundle `.js` → 103 pass, 0 fail. Excluded 4 browser bundles (shipped to browsers, not node): `modern-screenshot.umd.js`, `live-browser.js`, `live-browser-dom.js`, `detector/detect-antipatterns-browser.js`.
- `python3 docs/skill-review/test_review.py` → 34 tests OK. `python3 docs/skill-review/test_inventory.py` → 13 tests OK.
- Metadata: `command-metadata.json` parses (23 commands, aligned with the SKILL.md table); SKILL.md frontmatter parses (name impeccable, version 4.1.1, license claim Apache 2.0).

Static walkthroughs (not executed): live-mode authority flow, hook triage flow, detector two-tier surfacing, concept-roll contract. Kept separate from executed tests above.

## Artifacts written (owned scope only)

- `docs/skill-review/library/harness-pi-agent-skills/impeccable/fa8118279b98/REVIEW.md` (new)
- `…/fa8118279b98/review.json` (new; status reviewed, disposition retain, pack optional-frontend-craft, 148 reviewedFiles, 4 change records covering all 7 changed paths, 6 questions, 5 validation entries, 6 limitations)
- 5 baseline files extended by comment-only question IDs (no behavior change): SKILL.md (×2), reference/live.md, reference/hooks.md, scripts/generate-image.mjs, scripts/concept-seed.mjs
- `PR_DESCRIPTION.md` at worktree root (dispatcher-facing summary)

## Limitations

- No live harness evaluation (no browser, dev server, simulator, model run).
- Upstream unknown; external CLI and tests/ fixtures unverified.
- No license text in-tree: no redistribution permission granted.
- Vendored `modern-screenshot.umd.js` hash-preserved, not line-audited.
- Graph tools unavailable; evidence is source inspection with exact paths.
- Same agent authored and verified; no independent second-model review.
- `review.py verify-preservation` global live gate not re-run here (frozen
  baseline + known Cursor-manifest drift are pre-recorded in VERIFICATION.md;
  this ticket changed only explicitly owned paths).
