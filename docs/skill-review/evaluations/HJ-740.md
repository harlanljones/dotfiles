# HJ-740 Evidence — harness-gemini-skills/impeccable/ea77c0b5c398

Date: 2026-09-09. Scope: Linear HJ-740, the one frozen 148-file full-tree variant
`impeccable:ea77c0b5c3987755bfbaf2f5464043ee49fa2005ca62343c9b65ba4f48c56553`.
Worker: opencode `muse-spark-1.3-contributor-free` (agent-tier:3-expert) in Herdr
worktree `/home/harlan/.local/share/ft/HJ-740-review-gemini`. Prior worker
checkpoint resumed, not redone: 148-file map, mirror hashes 148/148, node
syntax 107 pass, safe fixtures 9/9 (dispatcher comment `b95d1ef8`). Linear was
read (issue, 5 comments, outgoing `blocks HJ-853`) and never mutated.

## Coverage Passes (148/148 files)

- Pass M (markdown, 40/40): `SKILL.md` (v4.1.1) plus all 39 `reference/`
  playbooks read in full — 35 topical (incl. 806-line `critique.md`, 416-line
  `document.md`, 323-line `live.md`, 336-line `harden.md`) and 4 `degraded/`
  fallbacks.
- Pass J (metadata, 1/1): `scripts/command-metadata.json` parses; 23 commands.
- Pass S (scripts, 107/107): per-file static scan of every `.mjs`/`.js`
  (lines, imports, exports, fs-write/net/proc/env capability flags) plus full
  reads of provider, target-slug/args, accept-verify, event-validation,
  detect facade/entry, hook trio head, hook-admin manifests, pin, critique
  storage, surface-brief, context head, doctor/concept-seed/embed-prompt/
  generate-image/copy-edit-agent heads, live-server vendored-script route,
  and the three browser-side bundles' headers.
- Pass P (provider diff): file-by-file diff against Claude sibling
  `harness-claude-skills/impeccable/6c5d5e55dd40` — identical path sets,
  22 files differ; all but five deltas are mechanical path/context-file swaps.

## Executed Checks (from repo root / worktree)

- `review.py check --source harness-gemini-skills --skill impeccable`:
  `allSelectedContractsValid: true` (1 selected, 0 errors, 0 missing files).
- Frozen-hash comparison over all 148 baseline entries: 148 checked;
  7 candidate diffs — exactly the 7 intended one-line question insertions;
  0 mirror mismatches against `/home/harlan/.gemini/skills/impeccable`,
  so every original byte is intact and text-provenance holds.
- `node --check` over all 107 scripts: 107 pass, 0 fail.
- Relative markdown-link walk over all 40 markdown files: 87 checked, 0 missing.
- Pure-function fixtures (`target-slug`, `event-validation`, `accept-verify`,
  `provider`): 9/9 assertions pass, zero writes, zero network.

## Static Walkthroughs (not executed)

Critique dual-agent/degraded-banner flow; live generate/accept/carbonize loop
with `accept-verify.mjs` gating `live-complete.mjs`; `hooks on` on a
Gemini-only project repairing nothing; copy-edit Apply with no codex/claude
CLI returning null to the degraded inline path. No browser, dev server, image
generation, hook install, or detector run against live targets was performed.

## Findings Worth Keeping

Provider build is deliberate: AskUserQuestion becomes direct user questions
(Gemini CLI has no native question tool); `GEMINI.md` replaces `CLAUDE.md`;
provider id `gemini`, `/` prefix, `.gemini/skills` paths. Gaps recorded as
six inline questions: frontmatter license/invocation metadata, Gemini hook
target, image cost disclosure, update phone-home default, vendored bundle
provenance, codex/claude-only copy-edit agent. License: no LICENSE file;
only two Apache-2.0 SPDX headers (Paul Bakaus) in detector facades.
Servers bind loopback only with token auth and strict event validation.
`craft-floor.md` carries two hover-animation lines the Claude tree lacks;
`new-work.md` omits the Claude tree's rendition-prior paragraph.

## Boundaries

Review readiness, not approval. No install, apply, stage, commit, push, PR,
paid model calls, or live-service runs. Owned writes only: the 7 candidate
edits plus `REVIEW.md`/`review.json`, this record, and root `PR_DESCRIPTION.md`.
