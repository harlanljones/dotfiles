# Engineering Authoring Candidates

Scope: only these `pack-mattpocock` identities in the frozen inventory:
`migrate-to-shoehorn`, `scaffold-exercises`, `setup-pre-commit`,
`setup-ts-deep-modules`, `teach`, `wait-what`, `wizard`, `writing-beats`,
`writing-for-agents`, `writing-fragments`, `writing-shape`.

Apply `RUBRIC.md` to each complete tree, including supporting code, metadata,
examples and licenses. Write per-candidate `REVIEW.md` and `review.json` with
reviewed paths, specific changes, retained expertise, pack/invocation suggestions,
per-skill execution alternatives, inline question IDs and validation limits.
Preserve useful writing pedagogy and technical gotchas. Treat invocation metadata
and context-load claims as provider-specific, not universal facts. Scale setup
and discovery to risk; installation, publication and execution require authority.
Wizard output is not permission to run destructive or credential-handling steps.

Inventory dependency and limits are in `VERIFICATION.md`. Own only these exact
candidate directories. Do not execute generated wizards/installers or change live
skills, settings, hooks, activation, staging, commits, pushes or Herdr topology.
Driver independently verifies candidate readiness; human approval remains open.
