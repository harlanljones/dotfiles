# Engineering Planning Candidates

Scope: only these `pack-mattpocock` identities in the frozen inventory:
`ask-matt`, `codebase-design`, `domain-modeling`, `grill-me`, `grill-with-docs`,
`improve-codebase-architecture`, `loop-me`, `setup-matt-pocock-skills`,
`to-questionnaire`, `to-spec`, `to-tickets`, `triage`, `wayfinder`.

Read all retained files. Apply `RUBRIC.md` and write per-candidate `REVIEW.md`
and `review.json`, recording specific source changes, retained expertise,
invocation/pack and execution alternatives, validation, provider/update limits,
and visible inline question IDs. Preserve licenses/assets and every supporting
reference; planning or research does not itself authorize tracker publication,
repo edits, commits or deployment. Retain the distinctions among decisions,
implementation, human review and deployment. Use configured tracker destinations,
not automatic GitHub/local substitutions or unverified assignment locks.

Dependencies and live drift are in `VERIFICATION.md`. Own only these exact
candidate directories. No live changes, installs, apply, staging, commits, push,
or Herdr topology. Candidate readiness requires independent driver verification.
