# Project Doc Planner Candidate Review

Disposition: retain critic/planner expertise with explicit document-write authority
and proportional delegation. Proposed pack: `planning-documents`, optional.
Invocation: task-triggered for project assessment and execution planning, not
ordinary editing. Execution: two analytical passes in-session for small inputs;
bounded critic/planner delegation for large independent evidence sets. One writer
owns guidance files; read-only assessment needs no Herdr or worktree.

## Full-Tree Evidence

Read all 75 baseline lines of `SKILL.md` and all four lines of
`agents/openai.yaml`. Retain display name, short description and default prompt;
the prompt requests a plan, not permission to alter policy files. The inventory
entry for this directory records source hashes and mirrors. No license discovered.

Preserved expertise: evidence-backed critique, anchored 0-4 scorecard, no averaging
away critical gaps, durable guidance versus temporary roadmap, metric baselines,
targets/instruments/owners/cadence, dependency graph and critical path, exclusive
write ownership, traceability, measurable milestone exits and invented-fact checks.

Baseline line 15 makes explicit invocation plus a writable directory authority
to change `AGENTS.md`. Candidate derives write authority from the actual request.
Lines 17-19 and 61 require parallel agents for all inputs; candidate preserves
the critic and planner roles while making delegation proportional. Reconciliation
operates on proposed content in read-only mode rather than accidentally granting
the driver writes. Completion requires mapped material findings and measurable
gates, not the mere presence of two files or exhausted tool calls.

## Validation and Questions

Static walkthroughs: a read-only assessment returns proposals; a request to write
guidance permits only named files; two delegated roles have one writer; invented
metrics become explicit unknowns; major findings map to tasks/decisions/risks;
unresolved human decisions do not masquerade as implementation readiness.

`Q-project-doc-planner-write` recommends file-write requests rather than invocation
alone as authority. `Q-project-doc-planner-delegation` recommends retaining two
roles without mandatory two-agent execution. User agency remains central: neither
recommendation turns the resulting plan into implementation/deployment authority.

## Maintenance and Limits

No project assessment was executed against an external project and no guidance
outside this candidate was changed by its workflow. Provider-specific metadata
support and live comparative quality remain untested. Tracked local ownership and
mirror mappings remain in the inventory; the `portableLocal`/explicit-link
distribution mismatch is integration work, not repaired by this review. Promote
only after approval and cross-harness discovery checks.
