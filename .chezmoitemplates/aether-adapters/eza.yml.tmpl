colourful: true

filekinds:
  normal: { foreground: "${foreground}" }
  directory: { foreground: "${blue}" }
  symlink: { foreground: "${cyan}" }
  pipe: { foreground: "${muted}" }
  block_device: { foreground: "${yellow}" }
  char_device: { foreground: "${yellow}" }
  socket: { foreground: "${muted}" }
  special: { foreground: "${magenta}" }
  executable: { foreground: "${green}" }
  mount_point: { foreground: "${bright_cyan}" }

perms:
  user_read: { foreground: "${cyan}" }
  user_write: { foreground: "${magenta}" }
  user_execute_file: { foreground: "${green}" }
  user_execute_other: { foreground: "${green}" }
  group_read: { foreground: "${cyan}" }
  group_write: { foreground: "${yellow}" }
  group_execute: { foreground: "${green}" }
  other_read: { foreground: "${cyan}" }
  other_write: { foreground: "${red}" }
  other_execute: { foreground: "${green}" }
  special_user_file: { foreground: "${red}" }
  special_other: { foreground: "${red}" }
  attribute: { foreground: "${muted}" }

size:
  major: { foreground: "${cyan}" }
  minor: { foreground: "${magenta}" }
  number_byte: { foreground: "${foreground}" }
  number_kilo: { foreground: "${bright_cyan}" }
  number_mega: { foreground: "${cyan}" }
  number_giga: { foreground: "${yellow}" }
  number_huge: { foreground: "${red}" }
  unit_byte: { foreground: "${foreground}" }
  unit_kilo: { foreground: "${bright_cyan}" }
  unit_mega: { foreground: "${cyan}" }
  unit_giga: { foreground: "${yellow}" }
  unit_huge: { foreground: "${red}" }

users:
  user_you: { foreground: "${blue}" }
  user_root: { foreground: "${magenta}" }
  user_other: { foreground: "${cyan}" }
  group_yours: { foreground: "${bright_cyan}" }
  group_root: { foreground: "${magenta}" }
  group_other: { foreground: "${foreground}" }

links:
  normal: { foreground: "${bright_cyan}" }
  multi_link_file: { foreground: "${cyan}" }

git:
  new: { foreground: "${green}" }
  modified: { foreground: "${magenta}" }
  deleted: { foreground: "${red}" }
  renamed: { foreground: "${cyan}" }
  typechange: { foreground: "${cyan}" }
  ignored: { foreground: "${muted}" }
  conflicted: { foreground: "${yellow}" }

git_repo:
  branch_main: { foreground: "${muted}" }
  branch_other: { foreground: "${bright_cyan}" }
  git_clean: { foreground: "${selection}" }
  git_dirty: { foreground: "${magenta}" }

security_context:
  colon: { foreground: "${muted}" }
  user: { foreground: "${muted}" }
  role: { foreground: "${cyan}" }
  typ: { foreground: "${blue}" }
  range: { foreground: "${magenta}" }

file_type:
  image: { foreground: "${bright_cyan}" }
  video: { foreground: "${bright_cyan}" }
  music: { foreground: "${cyan}" }
  lossless: { foreground: "${cyan}" }
  crypto: { foreground: "${red}" }
  document: { foreground: "${foreground}" }
  compressed: { foreground: "${yellow}" }
  temp: { foreground: "${muted}" }
  compiled: { foreground: "${muted}" }
  build: { foreground: "${green}" }
  source: { foreground: "${magenta}" }

punctuation: { foreground: "${selection}" }
date: { foreground: "${yellow}" }
inode: { foreground: "${muted}" }
blocks: { foreground: "${muted}" }
header: { foreground: "${foreground}" }
octal: { foreground: "${yellow}" }
flags: { foreground: "${magenta}" }

symlink_path: { foreground: "${bright_cyan}" }
control_char: { foreground: "${yellow}" }
broken_symlink: { foreground: "${red}" }
broken_path_overlay: { foreground: "${red}" }
