[data]
machine = '{{ if eq .chezmoi.hostname "omarchy" }}augustus{{ else if eq .chezmoi.os "linux" }}augustus{{ else if eq .chezmoi.os "darwin" }}hadrian{{ else }}unknown{{ end }}'

[remove]
# Skip encrypted file — not using age encryption
encrypted_private_dot_linear.toml.age = true
