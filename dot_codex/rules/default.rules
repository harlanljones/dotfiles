# Deny git commit
prefix_rule(
    pattern = ["git", "commit"],
    decision = "forbidden",
    justification = "Direct commits are disabled. Please use the automated PR workflow."
)

# Deny git push
prefix_rule(
    pattern = ["git", "push"],
    decision = "forbidden",
    justification = "Direct pushes to remote branches are blocked in this environment."
)

